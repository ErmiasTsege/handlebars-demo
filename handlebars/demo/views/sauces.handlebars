<section>
    <h2>
        There are {{sauces.length}} sauces
    </h2>

    {{#each sauces}}
    <article class="clearfix item-container block"
    <h2>{{name}} </h2>
    <img class="item-img" src="{{image}}"/>
      <a class="btn" href="/sauces/{{id}}">View Sauce</a>
        <a class="btn" href="/saucename/{{name}}">View Sauce</a>
    {{/each}}
      </section>